{
	"name": "advanced-node",
	"version": "1.0.0",
	"description": "NodeJs Avançado com TDD, Clean Arch e Typescript",
	"main": "index.js",
	"scripts": {
		"start": "rm -rf dist/ && tsc && node dist/main/api",
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "rm -rf dist/ && tsc"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@types/module-alias": "^2.0.1",
		"@types/node": "^17.0.10",
		"typescript": "^4.5.4"
	},
	"engines": {
		"node": "16.x"
	},
	"dependencies": {
		"module-alias": "^2.2.2"
	}
}
